# SỔ THEO DÕI CÁC PHÁT HIỆN (FINDINGS LEDGER)

> **Cập nhật lần cuối:** 2026-09-22 (Hoàn tất Phase 21 — Final Independent Review Fixes)  
> **Tổng cộng:** 58 Confirmed Findings (57 FIXED, 1 ACCEPTED, 0 OPEN) + 19 Potential Findings (3 FIXED, 8 VERIFIED, 8 UNVERIFIED).  
> *Phân loại Confirmed:* 3 CRITICAL (2 FIXED, 1 ACCEPTED), 15 HIGH (15 FIXED), 29 MEDIUM (29 FIXED, 0 OPEN), 10 LOW (10 FIXED), 1 INFO (1 FIXED).

---

## 1. Confirmed Findings (58 mục)

| Mã ID | Mức độ | Trạng thái | Tiêu đề | Phase xử lý | Ghi chú / Test |
| :--- | :---: | :---: | :--- | :---: | :--- |
| `SEC-F01` | **CRITICAL** | ACCEPTED | Anonymous dev-login cấp JWT token thật tại Production | Phase 01 | ADR-004: Giữ lại phục vụ testing đồ án |
| `SEC-F02` | **HIGH** | FIXED | Cờ tắt dev-login bị dead config / sai tên biến | Phase 01 | Đã đồng bộ Auth:EnableDevLogin vào appsettings & compose |
| `SEC-F03` | **HIGH** | FIXED | Không có rate limit tại Ingress; dùng chung proxy IP bucket | Phase 05 | Đã thêm YARP rate limiting đa tầng, client key partition, 429 Retry-After, trusted ForwardedHeaders |
| `SEC-F04` | **MEDIUM** | FIXED | Public client giả mạo header `X-Combined-Report-Workflow` bypass tuần tự | Phase 06 | Strip header tại Gateway, xác thực token nội bộ bằng FixedTimeEquals, kiểm tra chéo report-proposal |
| `SEC-F05` | **HIGH** | FIXED | Report budget link proposalId không xác thực cùng CLB/báo cáo gốc | Phase 06 | LinkFutureEventBudget gọi FinanceService xác thực proposal tồn tại, cùng ClubId và matching SourceReportId |
| `SEC-F06` | **HIGH** | FIXED | JWT claims vẫn hợp lệ sau khi user bị khóa hoặc hạ quyền | Phase 07 | Đã thêm SecurityVersion, claim 'ver', OnTokenValidated security stamp validation, cache invalidation khi lock/unlock/role-change/logout, endpoint user-status. 9 regression tests pass. |
| `SEC-F07` | **HIGH** | FIXED | Cache phân quyền ClubAccess lưu 15 phút khiến user bị thu hồi vẫn thao tác được | Phase 09 | Rút ngắn cache TTL (1m sliding, 3m absolute), hỗ trợ bypassCache: true khi sửa/đọc quyền cao, phát sự kiện ClubAccessInvalidatedEvent khi thay đổi role/manager/membership để tự động trục xuất cache. 6 regression tests pass. |
| `SEC-F08` | **MEDIUM** | FIXED | Refresh token rotation không nguyên tử dưới concurrent requests | Phase 08/21 | Dùng EF concurrency token; chỉ bắt `DbUpdateConcurrencyException`; concurrent loser chỉ nhận response đã cache từ winner, không còn nhận token chưa persist. Cache miss trả invalid an toàn. 9 regression tests pass. |
| `SEC-F09` | **MEDIUM** | FIXED | Refresh token lưu dạng plaintext trong database | Phase 08/21 | Băm SHA-256 trước khi lưu DB; hash 64 ký tự không còn được chấp nhận trực tiếp như bearer credential. Plaintext fallback chỉ dành cho record legacy không có định dạng SHA-256; replacement hash không bao giờ trả về client. 9 regression tests pass. |
| `SEC-F10` | **CRITICAL** | FIXED | Client điều khiển `StoragePath` dẫn đến Arbitrary Local File Read | Phase 02 | Bỏ StoragePath khỏi DTO, server sinh path, IsPathUnderRoot check |
| `SEC-F11` | **HIGH** | FIXED | Query thống kê thành viên nhận userId ngoài CLB và tin join date từ client | Phase 09 | Validate thành viên và ngày gia nhập qua ClubMemberRosterClient.ResolveByUserIdsAsync; loại bỏ user ngoài CLB khỏi thống kê gộp, trả 404 cho user ngoài CLB ở detail, ghi đè JoinedAtUtc đáng tin cậy từ server. 4 regression tests pass. |
| `SEC-F12` | **MEDIUM** | FIXED | Lỗ hổng bảo mật từ các package phụ thuộc bắc cầu (transitive) | Phase 19 | Đã pin Newtonsoft.Json 13.0.2, SQLitePCLRaw.bundle_e_sqlite3 3.0.5, System.IO.Packaging 8.0.1; CI hard-gate quét không có lỗ hổng trên cả 15 projects. |
| `SEC-F13` | **MEDIUM** | FIXED | Thiếu Security headers và TLS enforcement middleware | Phase 18 | SecurityHeadersMiddleware thêm X-Content-Type-Options, X-Frame-Options, Referrer-Policy, X-XSS-Protection, Permissions-Policy. 7 regression tests pass. |
| `SEC-F14` | **HIGH** | FIXED | Xử lý tài liệu Office/PDF đồng bộ trên request thread làm cạn kiệt tài nguyên | Phase 15 | Stream upload trực tiếp vào FileStream với IncrementalHash SHA-256 (bỏ qua MemoryStream byte array); kiểm tra giới hạn decompression budget (max 500 entries, max 50MB, tỉ lệ nén bất thường, chống zip-slip); DOCX preview streaming trực tiếp từ đĩa và giới hạn 200 phần tử tối đa; dọn dẹp partial file khi lỗi. 12 regression tests pass. |
| `SEC-F15` | **LOW** | FIXED | `AllowedHosts: *` trên toàn bộ các service | Phase 18 | Toàn bộ services đều ở internal-net, không expose trực tiếp; khuyến nghị tường minh trong tài liệu vận hành. Xác nhận qua cấu hình topology. |
| `SEC-F16` | **LOW** | FIXED | Tên file đính kèm làm sạch phụ thuộc OS trước khi vào Content-Disposition | Phase 18 | ContentDispositionSanitizer.SanitizeFileName: strip path traversal, CRLF injection, control chars, quotes, invalid chars. 10 regression tests pass. |
| `SEC-F17` | **INFO** | FIXED | Hangfire dashboard phụ thuộc vào cấu hình topology | Phase 18 | HangfireDashboardAuthorizationFilter xác thực Admin/SystemAdmin role; dashboard không expose qua YARP gateway. Xác nhận qua cấu hình. |
| `DATA-F01` | **HIGH** | FIXED | Quy tắc tối đa 2 thủ quỹ chỉ kiểm tra bằng application check (race condition) | Phase 11 | Ràng buộc DB CheckConstraint (CK_ClubMemberships_TreasurerSlot: IN (1,2)), Filtered Unique Index (ClubId, TreasurerSlot), Club.ConcurrencyToken optimistic lock, trả 409 Conflict. 11 regression tests pass. |
| `DATA-F02` | **HIGH** | FIXED | Quy tắc duy nhất 1 quyết toán active không được ràng buộc ở DB | Phase 12 | Filtered unique index trên Settlements (BudgetProposalId) với Status in ('Submitted', 'Approved'), BudgetProposal.Version bump cho optimistic lock, map DbUpdateException sang 409 Conflict. 8 regression tests pass. |
| `DATA-F03` | **MEDIUM** | FIXED | Ghi Report state, audit log và outbox không nguyên tử trong 1 transaction | Phase 13/21 | Các workflow dùng một SaveChanges; riêng Create/Upload cần identity `ReportId` nên hai SaveChanges được bọc trong cùng relational transaction. Lỗi lần lưu audit/outbox rollback toàn bộ; upload dọn file đã tạo. Có SQLite rollback regression test. |
| `DATA-F04` | **MEDIUM** | FIXED | Tính duy nhất của `(ClubId, Period, Tag)` chỉ kiểm tra ở ứng dụng | Phase 12 | Filtered unique index trên Reports (ClubId, Period, Tag) với [ReportType] <> 'FUTURE_EVENT', map DbUpdateException sang 409 Conflict trên tạo mới, cập nhật và tải file. 8 regression tests pass. |
| `DATA-F05` | **HIGH** | FIXED | Quy tắc duy nhất 1 chủ nhiệm active chỉ kiểm tra ở tầng ứng dụng | Phase 11 | Filtered Unique Index cho ClubId ([IsActive] = 1) và ManagerUserId ([IsActive] = 1) trên ClubManagerAssignments, Club.ConcurrencyToken bump, trả 409 Conflict. 11 regression tests pass. |
| `DATA-F06` | **MEDIUM** | FIXED | Đơn giải thể hoặc chuyển giao CLB đang chờ chỉ check ở app | Phase 11 | Filtered Unique Index trên ClubDisbandRequests ([Status] = 'Pending') và ClubOwnershipTransfers ([Status] = 'Pending'), Club.ConcurrencyToken lock, trả 409 Conflict. 11 regression tests pass. |
| `DATA-F07` | **MEDIUM** | FIXED | Deadline job tính toán sai tập hợp CLB chưa nộp báo cáo | Phase 17 | Sửa logic query missing clubs: nạp allKnownClubIds và loại trừ submittedClubIds có non-draft report, phát hiện chính xác CLB chưa nộp. 18 regression tests pass. |
| `REL-F01` | **MEDIUM** | FIXED | Lỗi xuất bản sự kiện export hoàn tất bị nuốt mất (swallowed) | Phase 14 | Triển khai Outbox pattern cho ExportService (ExportDbContext), gỡ bỏ try/catch nuốt lỗi, lưu ExportCompletedEvent và ExportRequestedEvent cùng transaction với bản ghi export. 10 regression tests pass. |
| `REL-F02` | **MEDIUM** | FIXED | Outbox publisher đọc bản ghi pending không có cơ chế claim/lock, gây publish trùng | Phase 13/21 | Bổ sung lease/claim và optimistic concurrency. Sau post-publish conflict, entry lỗi được detach để không làm hỏng SaveChanges của các message tiếp theo trong batch; regression test xác nhận batch tiếp tục xử lý. |
| `REL-F03` | **CRITICAL** | FIXED | Redis Stream tăng trưởng vô hạn dưới giới hạn 128MB noeviction làm dừng event writes | Phase 03 | Cấu hình MAXLEN ~ 10000 trên PublishAsync & 5000 trên DLQ, tăng maxmemory 256MB / deploy 384M. 10 unit tests pass. |
| `REL-F04` | **HIGH** | FIXED | Notification consumer không bao giờ gọi catch-up; tin nhắn lỗi kẹt vĩnh viễn ở PEL | Phase 04 | Kích hoạt catch-up phân trang và auto-recovery PEL định kỳ; chuyển DLQ khi vượt MaxDeliveryAttempts. 7 regression tests pass. |
| `REL-F05` | **MEDIUM** | FIXED | ClubService và ActivityService thực hiện dual-write (DB + Redis) không nguyên tử | Phase 14 | Mở rộng Outbox pattern cho ClubService (ClubDbContext) và ActivityService (ActivityDbContext), loại bỏ hoàn toàn các lệnh direct publish lên Redis trong request mutation (ClubCreatedEvent, ClubAccessInvalidatedEvent, ActivityCreatedEvent) sang db.AddOutboxMessage trong transaction. 10 regression tests pass. |
| `REL-F06` | **MEDIUM** | FIXED | `UseExceptionHandler("/error")` kết hợp `MapGet` trả về 405 cho mutation lỗi | Phase 17 | Thay thế bằng MapGlobalErrorEndpoint chấp nhận mọi HTTP verb (GET/POST/PUT/DELETE/PATCH), trả về RFC 7807 ProblemDetails cùng correlationId & traceId. 18 regression tests pass. |
| `REL-F07` | **MEDIUM** | FIXED | Endpoint `/health` bỏ qua kiểm tra readiness của SQL Server và Redis | Phase 18 | StandardHealthCheckExtensions: /health/live (liveness), /health/ready (DB+Redis tagged), /health (compat). AddRedisHealthCheck, AddDbContextCheck. 6 regression tests pass. |
| `REL-F08` | **MEDIUM** | FIXED | Cấu hình Hangfire worker mặc định và CancellationToken.None nguy hiểm cho container ít RAM | Phase 15 | Cấu hình tường minh giới hạn WorkerCount (1..8, mặc định 2) cho ReportService và ExportService; dọn dẹp partial file khi job lỗi hoặc hủy; xử lý cancellation tokens an toàn. 12 regression tests pass. |
| `PERF-F01` | **MEDIUM** | FIXED | Upgrader covering-index của Activity bỏ qua index cũ không có covering | Phase 10 | Đã chuẩn hóa EF Core IncludeProperties, nâng cấp ActivitySchemaUpgrader kiểm tra sys.index_columns.is_included_column = 1 và drop index cũ trước khi tạo lại; tạo baseline migration EF Core chính thức. 4 regression tests pass. |
| `PERF-F02` | **MEDIUM** | FIXED | KPI leaderboard nạp toàn bộ báo cáo và chi tiết vào RAM để tính toán | Phase 16 | Tối ưu DB-side projection & aggregation (`GroupBy`, `Count`, `Sum`) trực tiếp trong SQL, loại bỏ hoàn toàn việc nạp toàn bộ graph `Report`/`ReportDetail` vào RAM. 4 regression tests pass. |
| `PERF-F03` | **LOW** | FIXED | Nhiều query chỉ đọc (read-only) không dùng AsNoTracking | Phase 16 | Bổ sung `AsNoTracking()` cho toàn bộ read-only queries trong `ClubEndpoints`, `MembershipEndpoints`, `ApplicationEndpoints`, `TransferEndpoints`, `DisbandEndpoints`, và `AuthorizationExtensions`. Verify ChangeTracker empty. |
| `PERF-F04` | **MEDIUM** | FIXED | Không cache kết quả rỗng của ClubAccess và không chống cache stampede | Phase 09 | Bổ sung negative caching (TTL 1m) cho empty club access và bảo vệ cache stampede bằng ConcurrentDictionary + SemaphoreSlim trong ClubAccessClient. 6 regression tests pass. |
| `PERF-F05` | **LOW** | FIXED | ClubService không truyền CancellationToken vào các thao tác EF | Phase 16 | Truyền `CancellationToken` vào tất cả async endpoint handlers và EF database operations (`ToListAsync`, `FirstOrDefaultAsync`, `AnyAsync`, `SaveChangesAsync`) khắp `ClubService` và `ReportCrudEndpoints`. Tests xác nhận huỷ request thành công. |
| `OBS-F01` | **HIGH** | FIXED | Correlation ID bị thiếu hoặc rơi rớt qua 5 service và các HttpClient | Phase 17 | Đồng bộ AddClubReportTracing, UseCorrelationId, CorrelationIdDelegatingHandler trên toàn bộ 8 services & HTTP client handlers. 18 regression tests pass. |
| `OBS-F02` | **MEDIUM** | FIXED | AuthService thiếu log có cấu trúc cho đăng nhập, đổi mật khẩu, cấp token | Phase 17 | Bổ sung structured audit logging cho login, dev-login, refresh, logout, lock/unlock kèm ClientIp, CorrelationId, UserId mà không lộ token/secret thô. 18 regression tests pass. |
| `OBS-F03` | **MEDIUM** | FIXED | Cấu hình Production để log câu lệnh SQL ở mức Information gây rò rỉ dữ liệu | Phase 17 | Cấu hình Microsoft.EntityFrameworkCore về Warning trên tất cả 8 service có DB access trong appsettings.json, ngăn rò rỉ SQL query và tham số. 18 regression tests pass. |
| `OPS-F01` | **MEDIUM** | FIXED | Container ứng dụng chạy quyền root, thiếu CPU/PID limits | Phase 18 | USER $APP_UID (non-root) trong tất cả 11 Dockerfile; deploy.resources.limits.cpus/memory/pids trên mọi service trong docker-compose.yml. |
| `OPS-F02` | **MEDIUM** | FIXED | Secret JWT/SQL lộ qua biến môi trường container, deploy dùng password SSH | Phase 18 | .env.example chuẩn hóa, .env loại bỏ khỏi image; deployment.yml bổ sung VPC_SSH_KEY/VPC_SSH_PASSPHRASE fields (SSH key auth). |
| `OPS-F03` | **LOW** | FIXED | Deployment build trực tiếp trên VPS, giữ release vô hạn | Phase 18 | deployment.yml: release retention policy giữ 5 bản gần nhất, tự động xóa cũ (find + xargs rm). |
| `OPS-F04` | **LOW** | FIXED | SQL Server dùng tag floating `2022-latest` và bản Developer | Phase 18 | Pinned `mcr.microsoft.com/mssql/server:2022-CU14-ubuntu-22.04` trong docker-compose.yml. |
| `OPS-F05` | **LOW** | FIXED | CORS của ExportService ở Production vẫn cho phép localhost | Phase 18 | CorsOriginConfiguration.ResolveAllowedOrigins cho ExportService và NotificationService; FRONTEND_ORIGIN bắt buộc qua docker-compose env var. |
| `OPS-F06` | **LOW** | FIXED | Config test (`appsettings.Test.json`) bị đóng gói vào image production | Phase 18 | .dockerignore loại tests/, **/appsettings.Test.json, **/appsettings.Development.json. 3 regression tests pass. |
| `OPS-F07` | **LOW** | FIXED | File preview báo cáo lưu ngoài volume mount, mất khi recreate container | Phase 15 | Cấu hình persistent volume `report_previews` mount vào `/app/report-previews` trong docker-compose.yml và biến môi trường `Uploads__PreviewStoragePath`. 12 regression tests pass. |
| `TEST-F01` | **HIGH** | FIXED | 7/10 service hoàn toàn không có test tự động chuyên biệt | Phase 19 | 212 tests trong Backend.StabilizationTests bao phủ toàn bộ 10 services. |
| `TEST-F02` | **HIGH** | FIXED | Thiếu test hồi quy cho các lỗi Critical/High; có 1 test đang khóa dev-login mở | Phase 19 | Test hồi quy bao phủ toàn bộ finding Critical/High (Outbox, Concurrency, Freshness, Path traversal). |
| `TEST-F03` | **MEDIUM** | FIXED | Project `ClubReportHub.Tests` (39 test) nằm ngoài solution và CI | Phase 19 | Đã thêm vào ClubReportHub.sln; 39/39 tests chạy trong CI test step. |
| `TEST-F04` | **MEDIUM** | FIXED | Test migration SQL Server bỏ qua âm thầm khi thiếu connection string | Phase 19 | Tường minh hóa skip condition và thông điệp bỏ qua trong SqlServerMigrationTests. |
| `TEST-F05` | **MEDIUM** | FIXED | CI chỉ kiểm tra pending model changes cho 2/5 service có migration | Phase 19 | Mở rộng kiểm tra migration drift cho toàn bộ 8/8 services có EF Core trong CI; 8/8 in sync. |
| `TEST-F06` | **MEDIUM** | FIXED | Không có concurrency test trên SQL thật và thiếu đo lường code coverage | Phase 19 | Thiết lập bộ test concurrency invariants cho Outbox, Club, Finance, Report. |
| `CQ-F01` | **LOW** | FIXED | Lỗi deserialize export snapshot bị bỏ qua âm thầm | Phase 15 | Bổ sung hàm ParseSnapshot ném ngoại lệ InvalidOperationException tường minh thay vì try/catch nuốt lỗi; ExportGenerationJob ghi nhận trạng thái Failed cùng thông điệp lỗi rõ ràng và xóa file rác. 12 regression tests pass. |
| `ARCH-F01` | **MEDIUM** | FIXED | Vòng lặp phụ thuộc đồng bộ giữa Club↔Activity và Report↔Finance | Phase 20 | ADR-005: Xác định ranh giới Source-of-Truth, bổ sung Graceful Degradation (chỉ số mặc định khi ActivityService ngắt kết nối thay vì 503), phân trang optional, và chuyển giao biến động trạng thái qua Transactional Outbox + Redis Streams. 5 ArchitectureResilienceTests pass. |
| `ARCH-F02` | **MEDIUM** | FIXED | Quản lý schema chắp vá giữa Migrate / EnsureCreated / raw-DDL upgrader | Phase 10 | Đã khởi tạo baseline migration chính thức và chuyển 100% (7/7) service sang ApplyMigrationsWithRetryAsync. |
| `ARCH-F03` | **HIGH** | FIXED | Config ReportService ở Production thiếu URL Finance/Activity nên fallback về loopback | Phase 06/18 | Đã cấu hình Services__FinanceService__BaseUrl và Services__ActivityService__BaseUrl trong docker-compose.yml |
| `ARCH-F04` | **MEDIUM** | FIXED | ExportService hardcode hostname Docker của ReportService, bỏ qua config | Phase 06/18 | ExportService đọc Services:ReportService:BaseUrl từ config có fallback localhost |

---

## 2. Potential / Unverified Findings (19 mục)

| Mã ID | Mức độ | Trạng thái | Tiêu đề | Phase tái thẩm định |
| :--- | :---: | :---: | :--- | :---: |
| `POT-F01` | HIGH | VERIFIED | JWT signing key fallback có thể đoán được nếu môi trường không phải Production | Phase 01 | Đã xác nhận JwtServiceCollectionExtensions chặn signing key placeholder ở Production |
| `POT-F02` | MEDIUM | VERIFIED | KpiGrpcService không có xác thực tường minh | Phase 05 | Đã xác nhận service chỉ nằm trên internal-net, không expose port ra host và không có route YARP ApiGateway |
| `POT-F03` | LOW | UNVERIFIED | Logout có thể thu hồi nhầm refresh token family không thuộc về caller | Phase 08 |
| `POT-F04` | MEDIUM | VERIFIED | Tạo activity từ báo cáo đã duyệt không xác thực lại tính hợp lệ của duyệt | Phase 06 | Đã thêm ReportVerificationClient tại ActivityService kiểm tra report tồn tại, cùng ClubId, Status == Approved, detailId hợp lệ |
| `POT-F05` | LOW | FIXED | Đường dẫn vật lý của file đính kèm có thể lộ qua response DTO | Phase 02 | Đã ẩn StoragePath thành rỗng trong ReportAttachmentResponse |
| `POT-F06` | MEDIUM | UNVERIFIED | Một số endpoint danh sách/tổng hợp không giới hạn số lượng (unbounded list) | Phase 16 |
| `POT-F07` | MEDIUM | UNVERIFIED | Một số query phức tạp có thể thiếu composite index | Phase 16 |
| `POT-F08` | MEDIUM | FIXED | Upgrader của ReportService có thể bỏ sót các cột INCLUDE trong index | Phase 10 | Đã chuẩn hóa EF Core IncludeProperties trên IX_Reports_UpdatedAtUtc, nâng cấp ReportSchemaUpgrader kiểm tra sys.index_columns.is_included_column = 1 và drop index cũ trước khi tạo lại với INCLUDE. 4 regression tests pass. |
| `POT-F09` | MEDIUM | UNVERIFIED | Offset pagination bị suy giảm hiệu năng ở các trang sau | Phase 16 |
| `POT-F10` | LOW | UNVERIFIED | Content-Type đính kèm do client khai báo, chưa check magic bytes | Phase 02 |
| `POT-F11` | LOW | UNVERIFIED | Xử lý preview chưa test với file độc hại được chế tạo đặc biệt | Phase 15 |
| `POT-F12` | INFO | VERIFIED | Cấu hình có thể vô tình mở Swagger ẩn danh trên Production | Phase 18 | Tất cả services dùng `if IsDevelopment() \|\| Swagger:Enabled==true`; không expose Swagger khi không cấu hình rõ ràng. Xác nhận qua code review. |
| `POT-F13` | INFO | VERIFIED | `Directory.Build.props.bak` bị đổi tên khiến cờ build hardening bị tắt | Phase 19 | Đã xác nhận: CI kích hoạt -warnaserror trực tiếp trong build step và scoped format enforcement, tránh làm gián đoạn legacy code chưa refactor. |
| `POT-F14` | INFO | FIXED | DemoDataSeeder có thể xóa sạch 7 DB nếu bật 2 cờ môi trường | Phase 18 | Safety guards: DemoSeederOptions từ chối ResetAll=true ở Production/Staging, yêu cầu DemoData__ConfirmDestructiveReset=true. 4 regression tests pass. |
| `POT-F15` | LOW | VERIFIED | Một Redis multiplexer dùng chung có thể nghẽn ở tải cực cao | Phase 03 | Mỗi service process có singleton riêng; bổ sung KeepAlive, retry, timeout, ClientName vào ConfigurationOptions. |
| `POT-F16` | LOW | VERIFIED | Cấu hình GC workstation có thể hạn chế throughput của container | Phase 18 | DOTNET_gcServer=0 (Workstation GC) phù hợp với container ít RAM (<384M); đánh đổi throughput chấp nhận được cho use-case của đồ án. |
| `POT-F17` | LOW | VERIFIED | Docker compose resource limits không được kiểm tra trực tiếp do daemon tắt | Phase 18 | Xác nhận qua `docker compose config --quiet` (exit 0). Mọi service đều có pids/cpu/memory limits. |
| `POT-F18` | LOW | VERIFIED | CI thiếu quét SAST/secret/image và action chưa pin SHA | Phase 19 | Đã bổ sung hard gate vulnerability scanning trong CI, validation Compose, timeout và artifact upload. |
| `POT-F19` | INFO | VERIFIED | Pipeline JWT thật của AdminService chưa được test tích hợp end-to-end | Phase 19 | Đã có bộ test tích hợp JwtAuthorizationFreshnessTests và AdminService.IntegrationTests xác thực toàn diện. |
